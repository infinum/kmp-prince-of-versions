//
//  CustomParserView.swift
//  iosApp
//
//  Created by Filip Stojanovski on 6.11.25.
//

import SwiftUI

struct CustomParserView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Custom Configuration Parser")
                .font(.headline)
            Text("TODO: Wire to your KMP custom parser example.")
                .foregroundStyle(.secondary)
            Spacer()
        }
        .padding(16)
        .navigationTitle("Custom Parser")
    }
}
